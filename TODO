-Move error-function back outside clojush.clj - right now the 'odd' example is hardcoded into clojush.clj 
-Adding instructions via :atom-generators is broken, fix
-Sevak-errors (but not when run on the local server), fix
-Remove sevak request output from being printed to stdout
-Error when num-islands doesn't evenly divide population size, fix
